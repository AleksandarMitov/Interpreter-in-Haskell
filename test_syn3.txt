var_name := 3452;
var_name2 := 342;
skip;
if((var_name <= 3434535) & (var_name2*2 <= var_name))
	then (
		skip;
		while(3 <= 5 & (var_name2*4 <= var_name)) do (
			call func_name;
			call func_name2;
			var_name3 := 45;
			//var_name2 := var_name2 + 1;
			begin
				var var_name4 := 34;
				var var_name5 := 45;
				proc func_name3 is (
					if(3 = 4)
						then skip
						else (
							skip;
							call ppp;
							call mlnop
						)
				);
				proc func_name is (
					x := 0;
					while(x <= 123) do (
						x := x + 1;
						var_name := var_name - 1;
						if (false) then skip
						else skip
					)
					)
				;
				proc func_name34 is
					if (true)
						then skip
						else skip;
				proc func_name4 is (
					if(3 = 4)
						then skip
						else (skip; skip; skip; skip)
					;
					while(4 <= 5) do (
						call ala_bala;
						mn  := 67;
						call myau
					)
				);
				skip;
				r := 34;
				skip
			end;
			skip;
			begin
				call jjj
			end;
			var_name2 := var_name2 + 1
		)
	)
	else
		skip
;
call lll;
call pos;
skip;
while ((4 = 4) & (((false & true)))) do (
	skip
);
skip
